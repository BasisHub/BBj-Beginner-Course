---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - docusaurus.config.ts
  - sidebars.ts
  - docusaurus.config.js
  - sidebars.js
  - src/css/custom.css
  - CLAUDE.md
  - .github/workflows/deploy.yml
autonomous: true

must_haves:
  truths:
    - "npm run build succeeds with TypeScript config files"
    - "npm run typecheck passes with no errors"
    - "All 5 plugins are configured in docusaurus.config.ts (search, mermaid, ideal-image, zoom, BBj syntax)"
    - "Color theme is blue (#2563eb) matching DWC tutorial"
    - "CLAUDE.md references npm commands and .ts config files"
  artifacts:
    - path: "docusaurus.config.ts"
      provides: "Full site configuration with all plugins"
      contains: "additionalLanguages.*bbj"
    - path: "sidebars.ts"
      provides: "Typed sidebar configuration"
      contains: "SidebarsConfig"
    - path: "tsconfig.json"
      provides: "TypeScript project configuration"
      contains: "@docusaurus/tsconfig"
    - path: "src/css/custom.css"
      provides: "Blue theme colors and external link icon CSS"
      contains: "#2563eb"
  key_links:
    - from: "docusaurus.config.ts"
      to: "sidebars.ts"
      via: "sidebarPath: './sidebars.ts'"
      pattern: "sidebarPath.*sidebars\\.ts"
    - from: "docusaurus.config.ts"
      to: "src/css/custom.css"
      via: "customCss: './src/css/custom.css'"
      pattern: "customCss.*custom\\.css"
    - from: ".github/workflows/deploy.yml"
      to: "package.json typecheck script"
      via: "npm run typecheck step"
      pattern: "npm run typecheck"
---

<objective>
Migrate the beginner tutorial build infrastructure to match the DWC tutorial: install 6 new packages, convert config files from JavaScript to TypeScript with full plugin configuration, switch theme to blue, add external link icon CSS, and update CLAUDE.md and CI workflow.

Purpose: Achieve infrastructure parity with the DWC tutorial so both BASIS training sites share identical tooling, plugins, and visual branding.
Output: Working TypeScript-based Docusaurus config with search, mermaid, ideal-image, zoom, and BBj syntax highlighting plugins configured. Old .js config files deleted.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create tsconfig.json</name>
  <files>package.json, tsconfig.json</files>
  <action>
1. Install the 4 new runtime dependencies and 2 new dev dependencies:
```bash
npm install @easyops-cn/docusaurus-search-local @docusaurus/theme-mermaid @docusaurus/plugin-ideal-image docusaurus-plugin-zooming
npm install -D @docusaurus/tsconfig typescript
```

2. Update package.json:
   - Change `"name"` from `"website"` to `"bbj-beginner-tutorial"`
   - Add `"typecheck": "tsc"` to the `scripts` object

3. Create `tsconfig.json` at project root with exact contents:
```json
{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": "."
  },
  "exclude": [".docusaurus", "build"]
}
```

4. Verify npm install succeeded: `npm ls @easyops-cn/docusaurus-search-local @docusaurus/theme-mermaid @docusaurus/plugin-ideal-image docusaurus-plugin-zooming typescript @docusaurus/tsconfig` should list all 6 without errors.
  </action>
  <verify>
- `npm ls --depth=0` shows all new packages installed
- `cat tsconfig.json` matches expected content
- `grep typecheck package.json` shows the script exists
- `grep bbj-beginner-tutorial package.json` shows the renamed package
  </verify>
  <done>All 6 new packages installed, package.json renamed with typecheck script, tsconfig.json exists and extends @docusaurus/tsconfig</done>
</task>

<task type="auto">
  <name>Task 2: Migrate config files to TypeScript with full plugin configuration</name>
  <files>docusaurus.config.ts, sidebars.ts, docusaurus.config.js, sidebars.js</files>
  <action>
1. Create `docusaurus.config.ts` with the EXACT content from the research document's "Complete docusaurus.config.ts (target state)" section. This includes:
   - TypeScript imports (`import type {Config}`, `import type * as Preset`)
   - `markdown.mermaid: true` and `markdown.hooks.onBrokenMarkdownLinks: 'warn'`
   - `themes` array with `@docusaurus/theme-mermaid` and `@easyops-cn/docusaurus-search-local` (using `require.resolve`)
   - `plugins` array with `@docusaurus/plugin-ideal-image` and `docusaurus-plugin-zooming`
   - `prism.additionalLanguages: ['java', 'bash', 'bbj']`
   - `themeConfig.mermaid` and `themeConfig.zooming` sections
   - `colorMode.defaultMode: 'light'` added
   - `satisfies Preset.Options` and `satisfies Preset.ThemeConfig` instead of JSDoc
   - NO `future.v4` flag
   - NO top-level `onBrokenMarkdownLinks`
   - `sidebarPath: './sidebars.ts'` (not .js)

   The complete target content is in `01-RESEARCH.md` under "Complete docusaurus.config.ts (target state)". Use it verbatim.

2. Create `sidebars.ts` with the EXACT content from the research document's "Complete sidebars.ts (target state)" section:
```typescript
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  tutorialSidebar: [
    { type: 'doc', id: 'introduction/index', label: 'Introduction' },
    { type: 'doc', id: 'getting-started/index', label: 'Set Up your Environment and Get Started' },
    { type: 'doc', id: 'object-oriented/index', label: 'Object Oriented Syntax in BBj' },
    { type: 'doc', id: 'file-io/index', label: 'File I/O and Data Access' },
    { type: 'doc', id: 'web-development/index', label: "Web Development with BBj's DWC" },
  ],
};

export default sidebars;
```

3. Delete the old JavaScript config files:
```bash
rm docusaurus.config.js sidebars.js
```

IMPORTANT structural notes (from research pitfalls):
- `@easyops-cn/docusaurus-search-local` MUST be in `themes` array, NOT `plugins`
- `@docusaurus/theme-mermaid` MUST be in `themes` array AND `markdown.mermaid` must be `true`
- `docusaurus-plugin-zooming` goes in `plugins` but its config goes in `themeConfig.zooming`
- `sidebarPath` MUST reference `'./sidebars.ts'` not `'./sidebars.js'`
  </action>
  <verify>
- `docusaurus.config.ts` exists and contains `additionalLanguages.*bbj`, `theme-mermaid`, `search-local`, `plugin-ideal-image`, `plugin-zooming`
- `sidebars.ts` exists and contains `SidebarsConfig`
- `docusaurus.config.js` does NOT exist
- `sidebars.js` does NOT exist
- `grep -c "future" docusaurus.config.ts` returns 0 (no v4 flag)
- `grep "sidebarPath" docusaurus.config.ts` shows `./sidebars.ts`
  </verify>
  <done>TypeScript config files created with all 5 plugins configured, BBj syntax highlighting enabled, old .js files deleted</done>
</task>

<task type="auto">
  <name>Task 3: Update CSS theme, CLAUDE.md, and CI workflow</name>
  <files>src/css/custom.css, CLAUDE.md, .github/workflows/deploy.yml</files>
  <action>
1. Replace `src/css/custom.css` entirely with the target content from the research document's "Complete custom.css (target state)" section. Key changes:
   - Light mode: green (#2e8555) -> blue (#2563eb) with full shade palette
   - Dark mode: teal (#25c2a0) -> light blue (#60a5fa) with full shade palette
   - Add external link icon CSS (::after pseudo-elements for http/https links)
   - Add dark mode variant of external link icon

2. Update `CLAUDE.md`:
   - Replace all `yarn` commands with `npm` equivalents:
     - `yarn` -> `npm install`
     - `yarn start` -> `npm start`
     - `yarn build` -> `npm run build`
     - `yarn serve` -> `npm run serve`
     - `GIT_USER=<username> yarn deploy` -> `GIT_USER=<username> npm run deploy`
   - Update "Key Configuration Files" section:
     - `docusaurus.config.js` -> `docusaurus.config.ts` with description: "Main config (site metadata, navbar, footer, plugins: local search, mermaid diagrams, ideal-image, zoom, Prism syntax highlighting with BBj/Java/Bash support)"
     - `sidebars.js` -> `sidebars.ts` with description: "Tutorial navigation structure (typed SidebarsConfig, 5 ordered sections)"
     - `src/css/custom.css` description: "Infima CSS framework customizations (blue theme matching DWC tutorial, external link icons)"
   - Add `file-io/` entry to Content Structure: `file-io/` - File I/O and record-oriented data access

3. Update `.github/workflows/deploy.yml`:
   - Add a "Type check" step BEFORE the "Build website" step:
   ```yaml
         - name: Type check
           run: npm run typecheck
   ```
   This goes after "Install dependencies" and before "Build website".
  </action>
  <verify>
- `grep "#2563eb" src/css/custom.css` returns a match (blue theme)
- `grep -c "#2e8555" src/css/custom.css` returns 0 (no green)
- `grep "external link icon" src/css/custom.css` returns a match
- `grep "npm" CLAUDE.md` returns matches; `grep "yarn" CLAUDE.md` returns 0
- `grep "docusaurus.config.ts" CLAUDE.md` returns a match
- `grep "typecheck" .github/workflows/deploy.yml` returns a match
- `grep -A1 "Type check" .github/workflows/deploy.yml` shows `run: npm run typecheck`
  </verify>
  <done>CSS uses blue theme with external link icons, CLAUDE.md references npm and .ts configs, CI runs typecheck before build</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run:
1. `npm run typecheck` -- must pass with no errors
2. `npm run build` -- must succeed and produce a `build/` directory
3. Confirm no old files remain: `ls docusaurus.config.js sidebars.js 2>&1` should show "No such file"
4. Confirm all plugins appear in config: `grep -c "theme-mermaid\|search-local\|ideal-image\|plugin-zooming\|bbj" docusaurus.config.ts` should be >= 5
</verification>

<success_criteria>
- `npm run typecheck` passes cleanly
- `npm run build` succeeds
- docusaurus.config.ts contains all 5 plugin configurations
- sidebars.ts exists with typed SidebarsConfig
- CSS uses blue (#2563eb) theme
- CLAUDE.md references npm commands
- CI workflow includes typecheck step
- No .js config files remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
