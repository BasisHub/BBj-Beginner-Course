---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "npm run build succeeds with zero errors"
    - "npm run typecheck passes with zero errors"
    - "BBj code blocks render with syntax highlighting distinct from plain text"
    - "Search bar is visible in the navbar"
    - "Mermaid diagram capability is configured (renders when content is added)"
    - "Image zoom and ideal-image plugins are loaded (active when images are added)"
  artifacts: []
  key_links: []
---

<objective>
Verify that the infrastructure migration from Plan 01-01 produces a working site. Run build and typecheck, start the dev server, and spot-check that all 5 plugins are functional.

Purpose: Catch any configuration errors before moving to Phase 2. A passing build is the gate for all future content work.
Output: Confirmed working build with all plugins verified. No file changes expected unless fixes are needed.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and typecheck</name>
  <files></files>
  <action>
1. Run `npm run typecheck` and capture output. Must exit 0 with no errors.
   - If TypeScript errors occur, fix them in the config files and re-run.

2. Run `npm run build` and capture output. Must exit 0 and produce a `build/` directory.
   - If build errors occur, diagnose from error output and fix.
   - Common issues to watch for (from research pitfalls):
     - "Cannot find module './sidebars.js'" -> sidebarPath not updated to .ts
     - Search bar missing -> search-local in plugins instead of themes
     - TypeScript error on require.resolve -> check tsconfig extends @docusaurus/tsconfig

3. Verify the build output:
   - `ls build/index.html` exists
   - `ls build/getting-started/index.html` exists (confirms docs route at root)

4. If any fixes were needed, document what was fixed and why in the summary.
  </action>
  <verify>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- `build/index.html` exists
- `build/getting-started/index.html` exists
  </verify>
  <done>Build and typecheck both pass. Build output contains expected HTML files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete infrastructure migration: TypeScript configs, 5 plugins (search, mermaid, ideal-image, zoom, BBj syntax), blue theme, external link icons, updated CLAUDE.md and CI workflow.
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm start` (opens browser automatically)

2. **BBj syntax highlighting**: Navigate to "Getting Started" page. BBj code blocks should have colored syntax (keywords, strings, comments in different colors), not monochrome plain text.

3. **Search**: Look for a search bar in the top navbar (or press Mod+K). Type a query like "BBj" -- it should show results from the docs.
   NOTE: Search indexing requires a production build. If search doesn't work on dev server, run `npm run build && npm run serve` and test on the served build instead.

4. **Blue theme**: The site should use blue accent colors (links, sidebar active item, navbar elements), not green/teal.

5. **External link icons**: The "BBj Documentation" link in the navbar and any external links in the footer should show a small arrow icon after the link text.

6. **Mermaid**: No mermaid content exists yet. This is verified by the build succeeding with the plugin configured. It will be tested when content is added.

7. **Image zoom / ideal-image**: No doc images exist yet. Verified by plugin loading without errors. Will be tested when images are added.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 success criteria (from ROADMAP.md):
1. Running `npm run build` succeeds with TypeScript config files -- verified in Task 1
2. Local search finds content when a user types a query -- verified in Task 2 spot-check
3. A mermaid code block renders as a diagram -- plugin configured, no content to test yet (deferred to content phases)
4. BBj code blocks render with syntax highlighting distinct from plain text -- verified in Task 2 spot-check
5. Image zoom and responsive image loading work -- plugins configured, no images to test yet (deferred to content phases)
</verification>

<success_criteria>
- `npm run typecheck` passes
- `npm run build` succeeds
- BBj code blocks show colored syntax highlighting
- Search bar is visible and functional
- Blue theme is applied across the site
- External link icons appear on http/https links
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
