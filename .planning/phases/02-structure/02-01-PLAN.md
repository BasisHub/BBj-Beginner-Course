---
phase: 02-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/samples.md
  - docs/01-introduction/index.md
  - docs/02-getting-started/index.md
  - docs/03-object-oriented/index.md
  - docs/04-error-handling/index.md
  - docs/05-strings-and-numbers/index.md
  - docs/06-collections/index.md
  - docs/07-file-io/index.md
  - docs/08-database-sql/index.md
  - docs/09-java-interop/index.md
  - docs/10-event-handling/index.md
  - docs/11-debugging/index.md
  - docs/12-web-development/index.md
  - sidebars.ts
  - docusaurus.config.ts
  - samples/
autonomous: true

must_haves:
  truths:
    - "All 12 numbered chapter directories exist under docs/ with index.md files"
    - "The 4 existing content sections (introduction, getting-started, object-oriented, file-io, web-development) have their content preserved in numbered directories"
    - "docs/index.md exists with slug: / and is the homepage (no content from old introduction)"
    - "docs/samples.md exists with runner documentation"
    - "Sidebar shows 4 collapsible categories with all 12 chapters"
    - "samples/ directory exists with 12 numbered subdirectories"
    - "npm run build succeeds with zero broken link errors"
  artifacts:
    - path: "docs/index.md"
      provides: "Homepage MDX stub with slug: /"
      contains: "slug: /"
    - path: "docs/samples.md"
      provides: "Running Samples documentation page"
      contains: "sidebar_position"
    - path: "sidebars.ts"
      provides: "Sidebar config with 4 autogenerated categories"
      contains: "autogenerated"
    - path: "docs/04-error-handling/index.md"
      provides: "Placeholder chapter page"
      contains: "Under Development"
  key_links:
    - from: "sidebars.ts"
      to: "docs/01-introduction/"
      via: "autogenerated dirName"
      pattern: "dirName.*01-introduction"
    - from: "docusaurus.config.ts"
      to: "docs/getting-started/"
      via: "footer link to: '/getting-started'"
      pattern: "to.*getting-started"
---

<objective>
Reorganize the flat docs/ directory into 12 numbered chapter directories, create placeholder pages for unwritten chapters, set up the sidebar with autogenerated categories, create a samples directory structure, and update all config references.

Purpose: This is the structural foundation for Phase 2. Every subsequent plan depends on the directory layout, sidebar config, and URL structure established here.

Output: 12 numbered chapter directories with index.md files, updated sidebars.ts with 4 categories, updated footer links, docs/index.md homepage stub, docs/samples.md runner page, and samples/ directory tree.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structure/02-RESEARCH.md
@sidebars.ts
@docusaurus.config.ts
@docs/introduction/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move existing content to numbered directories and create placeholders</name>
  <files>
    docs/index.md
    docs/01-introduction/index.md
    docs/02-getting-started/index.md
    docs/03-object-oriented/index.md
    docs/04-error-handling/index.md
    docs/05-strings-and-numbers/index.md
    docs/06-collections/index.md
    docs/07-file-io/index.md
    docs/08-database-sql/index.md
    docs/09-java-interop/index.md
    docs/10-event-handling/index.md
    docs/11-debugging/index.md
    docs/12-web-development/index.md
    docs/samples.md
  </files>
  <action>
    **Step 1: Create new docs/index.md (homepage stub)**

    Create `docs/index.md` as a minimal MDX homepage stub. This will be populated with React component imports in Plan 02-02. For now it needs valid frontmatter so the build passes:

    ```markdown
    ---
    sidebar_position: 1
    slug: /
    title: Introduction to BBj Development
    hide_table_of_contents: true
    ---

    # Introduction to BBj Development

    A hands-on course for experienced developers new to BBj.
    ```

    **Step 2: Move existing content directories**

    Move existing content to numbered directories. Preserve ALL content within each index.md file. Only update frontmatter:

    - `docs/introduction/` -> `docs/01-introduction/` (move the entire directory; update frontmatter to add `sidebar_position: 1` and `title: "Introduction"`. REMOVE the `slug: /` from this file since the homepage is now docs/index.md)
    - `docs/getting-started/` -> `docs/02-getting-started/` (add `sidebar_position: 1` and `title: "Getting Started"`)
    - `docs/object-oriented/` -> `docs/03-object-oriented/` (add `sidebar_position: 1` and `title: "Object-Oriented BBj"`)
    - `docs/file-io/` -> `docs/07-file-io/` (add `sidebar_position: 1` and `title: "File I/O and Data Access"`)
    - `docs/web-development/` -> `docs/12-web-development/` (add `sidebar_position: 1` and `title: "Web Development"`)

    After moving, delete the old empty directories (introduction/, getting-started/, object-oriented/, file-io/, web-development/).

    **Step 3: Create placeholder chapter directories**

    Create these 7 directories with index.md placeholder pages. Each placeholder uses this template (adapt title and description per chapter):

    ```markdown
    ---
    sidebar_position: 1
    title: "Chapter Title"
    ---

    # Chapter Title

    :::info Under Development
    This chapter is coming soon. It will cover [brief list of topics].
    :::

    In the meantime, see the official [BBj Documentation](https://documentation.basis.cloud/BASISHelp/WebHelp/bbjobjects/bbjobjects.htm).
    ```

    Placeholder chapters to create:
    - `docs/04-error-handling/index.md` - "Error Handling" - topics: SETERR, THROW, ON ERR, error codes, try/catch patterns
    - `docs/05-strings-and-numbers/index.md` - "Strings and Numbers" - topics: LEN, MID, POS, CVS, STR, NUM, MASK, string templates
    - `docs/06-collections/index.md` - "Collections" - topics: BBjVector, BBjHashMap, Java collections interop, iteration patterns
    - `docs/08-database-sql/index.md` - "Database and SQL" - topics: SQLOPEN, SQLPREP, BBjRecordSet, parameterized queries
    - `docs/09-java-interop/index.md` - "Java Interop" - topics: calling Java from BBj, using Java libraries, extending Java interfaces
    - `docs/10-event-handling/index.md` - "Event Handling" - topics: setCallback, event objects, process_events loop, common event types
    - `docs/11-debugging/index.md` - "Debugging" - topics: BBj IDE debugger, BEM, common error codes, PRINT-based debugging

    **Step 4: Create docs/samples.md**

    Create `docs/samples.md` as a standalone page explaining how to run the sample BBj files:

    ```markdown
    ---
    sidebar_position: 2
    title: "Running Samples"
    ---

    # Running the Sample Code

    This course includes runnable BBj sample files organized by chapter. Here's how to use them.

    ## Prerequisites

    - BBj installed and configured (see [Getting Started](/getting-started) for setup instructions)
    - Access to the BBj IDE or command-line interpreter

    ## Where to Find Samples

    Sample files are organized in the `samples/` directory at the root of this repository, grouped by chapter number:

    ```
    samples/
    ├── 01-introduction/
    ├── 02-getting-started/
    ├── 03-object-oriented/
    ├── ...
    └── 12-web-development/
    ```

    ## Running a Sample

    ### From the BBj IDE

    1. Open the BBj IDE
    2. Use **File > Open** to navigate to the sample `.bbj` file
    3. Click **Run** or press **F5**

    ### From the Command Line

    ```bash
    bbj path/to/sample.bbj
    ```

    :::tip
    Sample files are self-contained and can be run independently. Each file includes comments explaining what it demonstrates.
    :::

    :::note
    Sample files will be added as each chapter is written. Chapters marked "Under Development" do not yet have sample code.
    :::
    ```

    **IMPORTANT:** No explicit `slug` frontmatter is needed on moved docs. Docusaurus 3.x number prefix parsing strips the `01-`, `02-` prefixes from doc IDs and URLs automatically. `docs/02-getting-started/index.md` will have URL `/getting-started`.
  </action>
  <verify>
    Run `ls -la docs/` to confirm all 12 numbered directories plus index.md and samples.md exist.
    Run `ls docs/01-introduction/index.md docs/07-file-io/index.md docs/12-web-development/index.md` to confirm moved content exists.
    Verify old directories are gone: `ls docs/introduction/ 2>&1` should fail.
  </verify>
  <done>
    12 numbered chapter directories exist under docs/, each with an index.md. Existing content is preserved in its new location. 7 placeholder chapters have "Under Development" notices. docs/index.md and docs/samples.md exist. Old unnumbered directories are deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sidebars.ts and docusaurus.config.ts</name>
  <files>
    sidebars.ts
    docusaurus.config.ts
  </files>
  <action>
    **Step 1: Rewrite sidebars.ts**

    Replace the entire contents of `sidebars.ts` with the autogenerated category pattern from the research. The sidebar should have:

    1. `'index'` - Homepage (first item, outside categories)
    2. `'samples'` - Running Samples page (second item, outside categories)
    3. Category "Getting Started" (collapsible: true, collapsed: false):
       - `{type: 'autogenerated', dirName: '01-introduction'}`
       - `{type: 'autogenerated', dirName: '02-getting-started'}`
    4. Category "Core Language" (collapsible: true, collapsed: true):
       - `{type: 'autogenerated', dirName: '03-object-oriented'}`
       - `{type: 'autogenerated', dirName: '04-error-handling'}`
       - `{type: 'autogenerated', dirName: '05-strings-and-numbers'}`
       - `{type: 'autogenerated', dirName: '06-collections'}`
    5. Category "Data and Applications" (collapsible: true, collapsed: true):
       - `{type: 'autogenerated', dirName: '07-file-io'}`
       - `{type: 'autogenerated', dirName: '08-database-sql'}`
       - `{type: 'autogenerated', dirName: '09-java-interop'}`
       - `{type: 'autogenerated', dirName: '10-event-handling'}`
    6. Category "Tools and Web" (collapsible: true, collapsed: true):
       - `{type: 'autogenerated', dirName: '11-debugging'}`
       - `{type: 'autogenerated', dirName: '12-web-development'}`

    Use the exact pattern from 02-RESEARCH.md "Complete Sidebar Configuration" section.

    **Step 2: Update docusaurus.config.ts footer links**

    The footer has two Course links:
    - "Introduction" with `to: '/'` -- keep this (homepage URL unchanged)
    - "Getting Started" with `to: '/getting-started'` -- keep this (URL unchanged because number prefix stripping produces `/getting-started` from `02-getting-started/`)

    No footer changes needed if URLs are preserved by number prefix stripping. However, VERIFY by checking that the build succeeds with `onBrokenLinks: 'throw'`.

    **IMPORTANT:** The `dirName` values must use the FULL directory name with number prefix (e.g., `'02-getting-started'`), even though resulting doc IDs have the prefix stripped.
  </action>
  <verify>
    Run `npm run build` from the project root. It must exit 0 with no broken link errors. The `onBrokenLinks: 'throw'` setting will catch any mismatched references.
    Run `npm run typecheck` to verify TypeScript validity of sidebars.ts.
  </verify>
  <done>
    sidebars.ts has 4 autogenerated categories covering all 12 chapters. docusaurus.config.ts footer links still work. Both `npm run build` and `npm run typecheck` pass with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create samples directory structure</name>
  <files>
    samples/01-introduction/.gitkeep
    samples/02-getting-started/.gitkeep
    samples/03-object-oriented/.gitkeep
    samples/04-error-handling/.gitkeep
    samples/05-strings-and-numbers/.gitkeep
    samples/06-collections/.gitkeep
    samples/07-file-io/.gitkeep
    samples/08-database-sql/.gitkeep
    samples/09-java-interop/.gitkeep
    samples/10-event-handling/.gitkeep
    samples/11-debugging/.gitkeep
    samples/12-web-development/.gitkeep
    samples/README.md
  </files>
  <action>
    Create the `samples/` directory at the project root (outside `docs/`). Create 12 numbered subdirectories matching the chapter directories. Add a `.gitkeep` file in each subdirectory so empty directories survive git.

    Create `samples/README.md` with a brief explanation:

    ```markdown
    # BBj Sample Code

    This directory contains runnable BBj sample files organized by chapter.

    Each subdirectory corresponds to a chapter in the tutorial. Sample files will be added as chapters are written.

    See the [Running Samples](/samples) page in the tutorial for instructions on running these files.
    ```

    Use `mkdir -p` for all directories, then `touch .gitkeep` in each.
  </action>
  <verify>
    Run `ls -d samples/*/` to confirm all 12 subdirectories exist.
    Run `ls samples/01-introduction/.gitkeep` to confirm .gitkeep files exist.
    Run `cat samples/README.md` to confirm README exists.
  </verify>
  <done>
    samples/ directory exists at project root with 12 numbered subdirectories, each containing a .gitkeep file. samples/README.md explains the directory purpose.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 -- confirms no broken links, all sidebar references resolve, all docs have valid frontmatter
2. `npm run typecheck` exits 0 -- confirms sidebars.ts is valid TypeScript
3. All 12 docs directories exist: `ls -d docs/??-*/` produces 12 results
4. All 12 samples directories exist: `ls -d samples/??-*/` produces 12 results
5. Old unnumbered directories are gone: `ls docs/introduction/ 2>&1` fails
6. Homepage stub exists: `head -5 docs/index.md` shows `slug: /`
7. Samples page exists: `head -5 docs/samples.md` shows title
</verification>

<success_criteria>
- Every existing content page loads at its expected URL (introduction, getting-started, object-oriented, file-io, web-development)
- 7 placeholder chapter pages load with "Under Development" notices
- Sidebar has 4 collapsible categories with all 12 chapters visible
- `npm run build` passes with zero broken link errors
- `npm run typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-structure/02-01-SUMMARY.md`
</output>
