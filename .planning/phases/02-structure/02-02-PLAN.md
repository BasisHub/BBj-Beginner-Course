---
phase: 02-structure
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Hero/index.tsx
  - src/components/Hero/styles.module.css
  - src/components/ChapterCards/index.tsx
  - src/components/ChapterCards/styles.module.css
  - src/components/HomepageFeatures/index.tsx
  - src/components/HomepageFeatures/styles.module.css
  - docs/index.md
autonomous: true

must_haves:
  truths:
    - "Homepage at / displays a Hero banner with course title, subtitle, and CTA button linking to /introduction"
    - "Homepage displays 12 chapter cards organized in a grid, each linking to its chapter URL"
    - "Placeholder chapters show a visual 'Coming Soon' distinction on their cards"
    - "Homepage displays 3 value proposition feature cards (Read Legacy Code, Modern-First, Hands-On)"
    - "All components render correctly in both light and dark mode"
  artifacts:
    - path: "src/components/Hero/index.tsx"
      provides: "Hero banner component"
      exports: ["default"]
    - path: "src/components/Hero/styles.module.css"
      provides: "Hero component styles"
      contains: ".hero"
    - path: "src/components/ChapterCards/index.tsx"
      provides: "Chapter cards grid component with 12 chapters"
      exports: ["default"]
    - path: "src/components/ChapterCards/styles.module.css"
      provides: "Chapter card styles"
      contains: ".card"
    - path: "src/components/HomepageFeatures/index.tsx"
      provides: "Feature highlights component with 3 value props"
      exports: ["default"]
    - path: "src/components/HomepageFeatures/styles.module.css"
      provides: "Feature card styles"
      contains: ".feature"
    - path: "docs/index.md"
      provides: "Homepage MDX importing all 3 components"
      contains: "import Hero"
  key_links:
    - from: "docs/index.md"
      to: "src/components/Hero/index.tsx"
      via: "MDX import"
      pattern: "import Hero from '@site/src/components/Hero'"
    - from: "docs/index.md"
      to: "src/components/ChapterCards/index.tsx"
      via: "MDX import"
      pattern: "import ChapterCards from '@site/src/components/ChapterCards'"
    - from: "docs/index.md"
      to: "src/components/HomepageFeatures/index.tsx"
      via: "MDX import"
      pattern: "import HomepageFeatures from '@site/src/components/HomepageFeatures'"
    - from: "src/components/ChapterCards/index.tsx"
      to: "chapter URLs"
      via: "Link to prop with chapter slugs"
      pattern: "to=.*/(introduction|getting-started|object-oriented)"
---

<objective>
Create three React homepage components (Hero, ChapterCards, HomepageFeatures) and wire them into the docs/index.md homepage via MDX imports.

Purpose: These components transform the homepage from a plain markdown page into a navigable course overview with chapter cards, matching the DWC Course's visual pattern.

Output: 6 new component files (3 TSX + 3 CSS modules), updated docs/index.md with MDX imports.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structure/02-RESEARCH.md
@.planning/phases/02-structure/02-01-SUMMARY.md
@src/css/custom.css
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero, ChapterCards, and HomepageFeatures components</name>
  <files>
    src/components/Hero/index.tsx
    src/components/Hero/styles.module.css
    src/components/ChapterCards/index.tsx
    src/components/ChapterCards/styles.module.css
    src/components/HomepageFeatures/index.tsx
    src/components/HomepageFeatures/styles.module.css
  </files>
  <action>
    Create `src/components/` directory if it doesn't exist. Create 3 subdirectories: Hero/, ChapterCards/, HomepageFeatures/.

    **Hero component (`src/components/Hero/index.tsx`):**

    Follow the DWC Course pattern exactly (from 02-RESEARCH.md Pattern 2):
    - Import `ReactNode` type, `Link` from `@docusaurus/Link`, `Heading` from `@theme/Heading`, styles from `./styles.module.css`
    - Render a `<header>` with className `styles.hero`
    - Inside: `<div className="container">` wrapping:
      - `<Heading as="h1">` with text "Introduction to BBj Development"
      - `<p>` subtitle: "A hands-on course for experienced developers new to BBj — learn modern patterns, understand legacy code, and start building."
      - `<div>` with a `<Link>` button: `className="button button--primary button--lg"`, `to="/introduction"`, text "Start Learning"
    - Export as default function component returning `ReactNode`

    **Hero styles (`src/components/Hero/styles.module.css`):**
    ```css
    .hero {
      padding: 4rem 0;
      text-align: center;
    }

    .heroTitle {
      font-size: 3rem;
      font-weight: 800;
    }

    .heroSubtitle {
      font-size: 1.25rem;
      color: var(--ifm-color-content-secondary);
      max-width: 600px;
      margin: 0 auto 2rem;
    }

    .heroButtons {
      display: flex;
      justify-content: center;
      gap: 1rem;
    }
    ```

    **ChapterCards component (`src/components/ChapterCards/index.tsx`):**

    Define a `ChapterItem` type:
    ```typescript
    type ChapterItem = {
      title: string;
      slug: string;
      description: string;
      comingSoon?: boolean;
    };
    ```

    Create a `chapters` array with ALL 12 chapters. Mark chapters without content as `comingSoon: true`:
    - Ch 1: "Introduction", slug "/introduction", "Course orientation, prerequisites, and how to use this material."
    - Ch 2: "Getting Started", slug "/getting-started", "Set up your environment, write your first BBj program, and learn basic syntax."
    - Ch 3: "Object-Oriented BBj", slug "/object-oriented", "Classes, methods, inheritance, and OOP patterns in BBj."
    - Ch 4: "Error Handling", slug "/error-handling", "SETERR, THROW, ON ERR, error codes, and try/catch patterns.", comingSoon: true
    - Ch 5: "Strings and Numbers", slug "/strings-and-numbers", "Daily-use string and numeric functions: LEN, MID, POS, CVS, STR, NUM.", comingSoon: true
    - Ch 6: "Collections", slug "/collections", "BBjVector, BBjHashMap, Java collections interop, and iteration patterns.", comingSoon: true
    - Ch 7: "File I/O", slug "/file-io", "Record-oriented file access, string templates, and data channels."
    - Ch 8: "Database and SQL", slug "/database-sql", "SQL connections, prepared statements, and BBjRecordSet.", comingSoon: true
    - Ch 9: "Java Interop", slug "/java-interop", "Call Java classes from BBj, use Java libraries, and extend Java interfaces.", comingSoon: true
    - Ch 10: "Event Handling", slug "/event-handling", "setCallback, event objects, the process_events loop, and common event types.", comingSoon: true
    - Ch 11: "Debugging", slug "/debugging", "BBj IDE debugger, BEM, error codes, and troubleshooting techniques.", comingSoon: true
    - Ch 12: "Web Development", slug "/web-development", "Deploy BBj applications to the web with the Dynamic Web Client."

    Render using Infima grid (from 02-RESEARCH.md):
    - Wrap in a `<section>` with className styles.chapters
    - Inside: `<div className="container">`
    - Add a `<Heading as="h2">` saying "Course Chapters"
    - Use `<div className="row">` containing cards
    - Each card: `<div className={clsx('col col--4', styles.cardCol)}>`
    - Inside each col: a `<Link>` wrapping a card div with `styles.card` class
    - Card shows: chapter number (formatted as "Chapter N"), title, description
    - If `comingSoon`, add a small "Coming Soon" text badge and apply `styles.comingSoon` class (reduced opacity 0.7)
    - Import `clsx` from 'clsx' for conditional class composition

    **ChapterCards styles (`src/components/ChapterCards/styles.module.css`):**
    ```css
    .chapters {
      padding: 2rem 0;
    }

    .cardCol {
      margin-bottom: 1.5rem;
    }

    .card {
      display: block;
      border: 1px solid var(--ifm-color-emphasis-300);
      border-radius: var(--ifm-card-border-radius, 0.5rem);
      padding: 1.25rem;
      height: 100%;
      transition: border-color 0.2s ease;
    }

    .card:hover {
      border-color: var(--ifm-color-primary);
      text-decoration: none;
    }

    .comingSoon {
      opacity: 0.7;
    }

    .chapterNumber {
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--ifm-color-primary);
      margin-bottom: 0.25rem;
    }

    .cardTitle {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      color: var(--ifm-heading-color);
    }

    .cardDescription {
      font-size: 0.9rem;
      color: var(--ifm-color-content-secondary);
      margin-bottom: 0;
    }

    .badge {
      display: inline-block;
      font-size: 0.7rem;
      font-weight: 600;
      padding: 0.1rem 0.5rem;
      border-radius: 0.25rem;
      background-color: var(--ifm-color-emphasis-200);
      color: var(--ifm-color-content-secondary);
      margin-left: 0.5rem;
    }
    ```

    **HomepageFeatures component (`src/components/HomepageFeatures/index.tsx`):**

    Define a `FeatureItem` type:
    ```typescript
    type FeatureItem = {
      title: string;
      description: string;
    };
    ```

    Create a `features` array with 3 value props (from 02-CONTEXT.md):
    - "Read Legacy Code" / "Understand inherited BBj codebases across all four generations — character UI, Visual PRO/5, BBj GUI, and DWC."
    - "Modern-First" / "Learn current BBj patterns as the default way to write code, with legacy patterns explained as context you'll encounter."
    - "Hands-On" / "Every chapter includes runnable BBj sample files so you learn by doing, not just reading."

    Render using Infima grid:
    - Wrap in `<section>` with className styles.features
    - Inside: `<div className="container">`
    - Use `<div className="row">` containing feature cards
    - Each feature: `<div className={clsx('col col--4', styles.featureCol)}>`
    - Inside: a div with `styles.feature` class containing title (Heading as="h3") and description paragraph
    - Center-aligned text

    **HomepageFeatures styles (`src/components/HomepageFeatures/styles.module.css`):**
    ```css
    .features {
      padding: 2rem 0;
    }

    .featureCol {
      margin-bottom: 1.5rem;
    }

    .feature {
      text-align: center;
      padding: 1.5rem;
      border: 1px solid var(--ifm-color-emphasis-200);
      border-radius: var(--ifm-card-border-radius, 0.5rem);
      height: 100%;
    }

    .featureTitle {
      font-size: 1.25rem;
      margin-bottom: 0.5rem;
    }

    .featureDescription {
      color: var(--ifm-color-content-secondary);
    }
    ```

    **IMPORTANT patterns:**
    - Use `import type {ReactNode} from 'react';` (type-only import)
    - Use `@docusaurus/Link` for internal links (not `<a>` tags)
    - Use `@theme/Heading` for headings (not raw `<h1>`, `<h2>`)
    - Use CSS Modules (`.module.css`) for all styles
    - Use Infima CSS variables for theme-aware colors
    - All components export default function returning `ReactNode`
  </action>
  <verify>
    Run `ls src/components/Hero/index.tsx src/components/ChapterCards/index.tsx src/components/HomepageFeatures/index.tsx` to confirm all component files exist.
    Run `ls src/components/Hero/styles.module.css src/components/ChapterCards/styles.module.css src/components/HomepageFeatures/styles.module.css` to confirm all style files exist.
    Run `npm run typecheck` to verify TypeScript compiles.
  </verify>
  <done>
    6 component files exist (3 TSX + 3 CSS modules). Hero renders title, subtitle, and CTA. ChapterCards renders 12 cards with coming-soon distinction. HomepageFeatures renders 3 value prop cards. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire homepage to import and render all 3 components</name>
  <files>
    docs/index.md
  </files>
  <action>
    Replace the contents of `docs/index.md` (created as a stub in Plan 02-01) with the full MDX homepage. Follow the DWC Course pattern from 02-RESEARCH.md:

    ```mdx
    ---
    sidebar_position: 1
    slug: /
    title: Introduction to BBj Development
    hide_table_of_contents: true
    ---

    import Hero from '@site/src/components/Hero';
    import HomepageFeatures from '@site/src/components/HomepageFeatures';
    import ChapterCards from '@site/src/components/ChapterCards';

    <Hero />

    <HomepageFeatures />

    <ChapterCards />
    ```

    **Key details:**
    - MDX imports must be OUTSIDE the frontmatter block (after the closing `---`)
    - Each import on its own line, no blank lines between imports
    - One blank line between each component render
    - No wrapping div needed -- Docusaurus MDX handles layout
    - `@site/` prefix resolves to the project root

    **Do NOT include:**
    - A "Ready to Get Started?" bottom section (keep it clean for now, can be added later)
    - Any markdown content between or after components
    - A Link import (not needed -- Hero handles the CTA internally)
  </action>
  <verify>
    Run `npm run build` from project root. Must exit 0 with no errors.
    Verify by checking build output includes the homepage: `ls build/index.html`
  </verify>
  <done>
    docs/index.md imports and renders Hero, HomepageFeatures, and ChapterCards. `npm run build` succeeds. Homepage at / renders all 3 components.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 -- all component imports resolve, no broken links
2. `npm run typecheck` exits 0 -- all TSX files are valid TypeScript
3. All 6 component files exist under src/components/
4. docs/index.md has 3 MDX import statements
5. ChapterCards data has exactly 12 entries
6. Homepage build output exists: `build/index.html` contains "Introduction to BBj Development"
</verification>

<success_criteria>
- Homepage renders Hero with title, subtitle, and CTA button
- Homepage renders 12 chapter cards with coming-soon visual distinction
- Homepage renders 3 feature highlight cards
- All internal links from cards resolve to existing chapter pages
- Components use Infima grid, CSS Modules, and theme variables
- Both light and dark mode are supported via CSS variables
</success_criteria>

<output>
After completion, create `.planning/phases/02-structure/02-02-SUMMARY.md`
</output>
