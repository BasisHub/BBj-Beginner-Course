---
phase: 02-structure
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "npm run build exits 0 with zero broken link errors"
    - "npm run typecheck exits 0"
    - "Homepage at / renders Hero, ChapterCards, and HomepageFeatures"
    - "Sidebar shows 4 collapsible categories with all 12 chapters listed"
    - "Prev/next navigation chains through all chapters in reading order"
    - "Placeholder chapters display Under Development notice"
    - "All existing content pages load at their expected URLs"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 implementation: build integrity, sidebar navigation, homepage rendering, prev/next navigation chain, and visual correctness.

Purpose: Gate check before Phase 2 is marked complete. Ensures the structural reorganization works end-to-end before content work begins.

Output: Verified build, human-approved visual rendering, Phase 2 ready for completion.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structure/02-01-SUMMARY.md
@.planning/phases/02-structure/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and navigation chain verification</name>
  <files></files>
  <action>
    **Step 1: Run build and typecheck**

    Run `npm run typecheck` and `npm run build`. Both must exit 0.

    **Step 2: Verify all chapter URLs resolve**

    After build, verify these files exist in the `build/` directory (confirming all chapter URLs work):
    - `build/index.html` (homepage)
    - `build/samples/index.html` (samples page)
    - `build/introduction/index.html` (Ch 1)
    - `build/getting-started/index.html` (Ch 2)
    - `build/object-oriented/index.html` (Ch 3)
    - `build/error-handling/index.html` (Ch 4)
    - `build/strings-and-numbers/index.html` (Ch 5)
    - `build/collections/index.html` (Ch 6)
    - `build/file-io/index.html` (Ch 7)
    - `build/database-sql/index.html` (Ch 8)
    - `build/java-interop/index.html` (Ch 9)
    - `build/event-handling/index.html` (Ch 10)
    - `build/debugging/index.html` (Ch 11)
    - `build/web-development/index.html` (Ch 12)

    If any file is missing, report the specific missing URL.

    **Step 3: Verify prev/next navigation chain**

    Check the built HTML for prev/next links. The reading order should flow:
    Homepage -> Running Samples -> Introduction -> Getting Started -> Object-Oriented -> Error Handling -> Strings and Numbers -> Collections -> File I/O -> Database and SQL -> Java Interop -> Event Handling -> Debugging -> Web Development

    Spot-check by grepping a few build files for pagination links:
    - `grep -l 'pagination' build/introduction/index.html` (should have prev/next)
    - `grep -l 'pagination' build/web-development/index.html` (should have prev only, no next)

    **Step 4: Verify homepage contains component output**

    Check that `build/index.html` contains rendered component content:
    - Grep for "Start Learning" (Hero CTA button text)
    - Grep for "Course Chapters" or one of the chapter titles (ChapterCards)
    - Grep for "Read Legacy Code" (HomepageFeatures)

    Report any component that appears missing from the built output.
  </action>
  <verify>
    `npm run build` exits 0. All 14 build/*/index.html files exist. Homepage HTML contains Hero, ChapterCards, and HomepageFeatures content.
  </verify>
  <done>
    Build passes with zero errors. All 12 chapter URLs plus homepage and samples URL resolve. Prev/next navigation chain is intact. Homepage renders all 3 components.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 structural reorganization:
    - 12 numbered chapter directories with sidebar navigation
    - Homepage with Hero banner, 12 chapter cards, and 3 feature highlights
    - Samples directory structure and runner documentation page
    - Prev/next navigation chain through all chapters
  </what-built>
  <how-to-verify>
    1. Run `npm start` from the project root
    2. Open http://localhost:3000/BBj-Beginner-Course/ in your browser
    3. **Homepage check:**
       - Hero banner displays "Introduction to BBj Development" title, subtitle, and "Start Learning" button
       - 12 chapter cards are visible in a grid layout
       - Cards for unwritten chapters show "Coming Soon" visual distinction
       - 3 feature cards show "Read Legacy Code", "Modern-First", "Hands-On"
    4. **Sidebar check:**
       - Left sidebar shows 4 categories: "Getting Started" (expanded), "Core Language", "Data and Applications", "Tools and Web"
       - Expanding each category shows the correct chapters
       - "Running Samples" appears before the first category
    5. **Navigation check:**
       - Click "Start Learning" button -> navigates to Introduction chapter
       - Click through a few chapters using bottom prev/next navigation
       - Click a "Coming Soon" chapter card -> placeholder page with "Under Development" notice
    6. **Content check:**
       - Navigate to /getting-started -> existing content is intact
       - Navigate to /file-io -> existing content is intact
       - Navigate to /web-development -> existing content is intact
    7. **Dark mode check:**
       - Toggle dark mode (sun/moon icon in navbar) -> components render with appropriate dark colors
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` and `npm run typecheck` both exit 0
2. All 14 expected HTML files exist in build/
3. Homepage HTML contains Hero, ChapterCards, and HomepageFeatures content
4. Human approves visual rendering and navigation
</verification>

<success_criteria>
- Build passes with zero errors
- All chapter URLs resolve
- Homepage renders all 3 components visually
- Sidebar shows correct category structure
- Prev/next navigation works through the reading order
- Existing content is preserved and accessible
- Dark mode works
</success_criteria>

<output>
After completion, create `.planning/phases/02-structure/02-03-SUMMARY.md`
</output>
