---
phase: 03-existing-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/01-introduction/index.md
  - docs/02-getting-started/index.md
autonomous: true

must_haves:
  truths:
    - "Getting Started has no video-only instruction -- every concept has text-based explanation"
    - "All YouTube iframes in Getting Started are wrapped in collapsible details elements"
    - "Setup section includes a note that tooling has evolved (BDTStudio, VSCode extension)"
    - "BBj for Java/.NET Developers tables appear in Introduction, not Getting Started"
    - "Getting Started uses professional peer tone -- no 'Play with it', 'Can you enhance it?', etc."
  artifacts:
    - path: "docs/01-introduction/index.md"
      provides: "Introduction with comparison tables moved from Getting Started"
      contains: "BBj for Java"
    - path: "docs/02-getting-started/index.md"
      provides: "Getting Started with text-first content, collapsible videos, peer tone"
      contains: "<details>"
  key_links:
    - from: "docs/02-getting-started/index.md"
      to: "docs/01-introduction/index.md"
      via: "comparison tables moved, no broken anchor references"
      pattern: "BBj for Java"
---

<objective>
Rewrite Getting Started to be text-first with collapsible video supplements, move comparison tables to Introduction, and apply professional peer tone throughout.

Purpose: Getting Started is the first content readers encounter after the Introduction. It must work without video playback and speak to experienced developers as peers, not students.
Output: Updated `docs/01-introduction/index.md` with comparison tables, updated `docs/02-getting-started/index.md` with text-first content and collapsible videos.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-existing-content/03-CONTEXT.md
@.planning/phases/03-existing-content/03-RESEARCH.md
@docs/01-introduction/index.md
@docs/02-getting-started/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move comparison tables to Introduction and rewrite Introduction tone</name>
  <files>docs/01-introduction/index.md, docs/02-getting-started/index.md</files>
  <action>
**In `docs/01-introduction/index.md`:**

1. Rewrite the page with professional peer tone. Replace the current heading "Introduction - PLEASE READ FIRST!" with just "Introduction" or a more professional equivalent.

2. Rewrite "Structure of this Material" to be direct: explain this is a meta-course that links to official docs, not a hand-holding tutorial. Remove the fishing metaphor.

3. Rewrite "Who can use this course" section:
   - Remove the "September 2021" date reference
   - Remove "Check with your manager or mentor" — replace with a direct statement of prerequisites
   - Replace "You will need to have learned about" with "This material assumes familiarity with"
   - Keep the prerequisite list (programming, loops, OOP, IDE) but frame it as a straightforward statement

4. Add a new section **after** the existing content (before "Please Contribute!") titled "## BBj for Java/.NET Developers" — move the entire section from Getting Started:
   - "What is BBj?" subsection
   - "Key Terminology" table
   - "Common Tasks" table
   - "Further Reading" links

5. Keep the "Please Contribute!" section at the bottom.

**In `docs/02-getting-started/index.md`:**

Remove the entire "## BBj for Java/.NET Developers" section (from line 10 through line 43, which includes "What is BBj?", "Key Terminology", "Common Tasks", and "Further Reading"). The Getting Started page should start with the setup section after the page heading.
  </action>
  <verify>
- `npm run build` succeeds
- `docs/01-introduction/index.md` contains "BBj for Java" section with tables
- `docs/02-getting-started/index.md` does NOT contain "BBj for Java" section
- No broken links (build catches these)
  </verify>
  <done>Comparison tables live in Introduction. Introduction has peer tone. Getting Started no longer has orientation content.</done>
</task>

<task type="auto">
  <name>Task 2: Make Getting Started text-first with collapsible videos and peer tone</name>
  <files>docs/02-getting-started/index.md</files>
  <action>
Apply these changes throughout `docs/02-getting-started/index.md` (after Task 1's removal of comparison tables):

**1. Setup section:**
- Add a note after the existing setup link that tooling has evolved: "BBj development tooling has expanded since the video below was recorded. In addition to the Eclipse plug-in shown, [BDTStudio](https://www.basis.com/eclipseplug-ins) (a preconfigured Eclipse distribution) and a VSCode extension are now available options."
- Wrap the setup video iframe in `<details><summary>Watch the video: Setting Up Your Environment</summary>` ... `</details>` with required blank lines
- Remove "This video shows the steps." and "Follow these steps to set up your environment. When you're done, you should have BBj and Eclipse running on your computer." — they become redundant with the text-first approach
- Rewrite the heading to "## Setting Up Your Environment" (drop "Java, BBj and Eclipse")

**2. Hello World section:**
- Remove "Here is a video to get you inspired:" — replace with text-first explanation:
  - BBj's `MSGBOX` function displays a modal dialog. The simplest BBj program:
  - Show the code block (already exists)
  - Explain: `MSGBOX` shows the dialog, `RELEASE` terminates the program
  - Then the video in collapsible `<details>`
- Wrap the Hello World iframe in `<details><summary>Watch the video: A First Hello World</summary>` ... `</details>`
- Change "Read the documentation about these verbs to get familiar with the documentation." to a direct statement

**3. Variables section:**
- Replace "Watch this video about Variables in BBj:" with text-first explanation before the collapsible video
- Wrap the variables iframe in `<details><summary>Watch the video: Variables in BBj</summary>` ... `</details>`
- The cheat sheet content is solid — keep it as-is

**4. Better Hello World section:**
- Replace "Now it's time to write a better version..." with a direct introduction: "This example creates a window with a button that displays a message box when clicked."
- Wrap the iframe in `<details><summary>Watch the video: A Better Hello World</summary>` ... `</details>`
- Replace "Write the program in your IDE. I suggest you develop it step by step, like in the video, and play with the syntax" with "Create this program in your IDE, building it incrementally to understand each step."
- Remove "You can find a copy of the program in the Files-Section of this Section." (no files section exists)

**5. Loops section:**
- Replace "Now it's time to learn how you do Loops in BBj, and how you can create IF-statements." with a direct opening: "BBj provides standard control flow structures."
- Replace "Again, let's start with a short video:" — put the loop/IF code examples BEFORE the video
- Wrap the iframe in `<details><summary>Watch the video: Loops and IF Statements</summary>` ... `</details>`
- Replace "Now visit this documentation page and locate the keywords. Have a look around..." with "Explore the [BBj verbs reference](https://documentation.basis.cloud/BASISHelp/WebHelp/commands/index.htm) for additional control flow options."

**6. Input fields section:**
- Replace "Take some time to visit the documentation pages for each of these controls. Have a look at the method signatures..." with: "Review the documentation for each control type. The method signatures, available events, and sample code demonstrate typical usage patterns."
- Replace the masks note starting with "Please recognize that some topics" with a direct statement about BBj-specific documentation links

**7. Calculator section:**
- Replace "Create a new program in your IDE and copy this program into your editor. Examine and run it, and play with it." with "Copy this program into your IDE and run it. The calculator updates in real time via the `ON_EDIT_MODIFY` event."
- Replace "Can you enhance it? Add a button to clear the input in both fields." with a structured exercise:
  ```
  ### Exercise

  Add a "Clear" button that resets both input fields to zero. Use `setValue()` on each `BBjInputN` control.
  ```
- Keep the tip about `setValue()` but remove the "Find it in the documentation!" prompt

**8. More hints section:**
- Replace "Did you notice that (in GUI) you couldn't use the Tab-Key..." with a direct explanation: "By default, BBj GUI windows do not support Tab key navigation between controls. Enable keyboard navigation by setting the `$00010000$` flag..."
- Replace "Play with them. Can you make the Window also allow navigating the fields with the Enter-key?" with a structured exercise: "Exercise: Combine the keyboard navigation flag with the Enter-key navigation flag to enable both. Flags are combined by addition."

**MDX formatting rules for all `<details>` blocks:**
- `<summary>` content on a single line
- Blank line after `</summary>`
- Blank line before `</details>`
  </action>
  <verify>
- `npm run build` succeeds
- All 5 YouTube iframes in Getting Started are wrapped in `<details>` blocks
- No instances of "Play with it", "Can you enhance it?", "Here is a video to get you inspired", "Watch this video about"
- Text-based explanations exist before every collapsible video
- The setup section mentions BDTStudio and VSCode
  </verify>
  <done>Getting Started is text-first with all videos in collapsible containers, peer tone applied throughout, setup section notes tooling evolution.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 with no broken link errors
2. `docs/01-introduction/index.md` contains "BBj for Java" heading and both tables (Key Terminology, Common Tasks)
3. `docs/02-getting-started/index.md` contains exactly 5 `<details>` blocks (one per video)
4. `docs/02-getting-started/index.md` does NOT contain "BBj for Java/.NET Developers"
5. No instances of casual prompts: grep for "Play with", "Can you enhance", "Have a look", "Let's start", "Now it's time"
</verification>

<success_criteria>
- Getting Started works as a complete text-based tutorial without video playback
- Videos are supplementary and collapsed by default
- Comparison tables live in Introduction where they serve as orientation
- All prose uses professional peer tone
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-existing-content/03-01-SUMMARY.md`
</output>
