---
phase: 06-differentiators-and-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/07-file-io/02-legacy-code.md
  - docs/07-file-io/index.md
  - docs/08-database-sql/04-legacy-code.md
  - docs/08-database-sql/01-connecting.md
  - docs/08-database-sql/02-queries.md
  - docs/08-database-sql/03-patterns.md
  - docs/09-java-interop/04-legacy-code.md
  - docs/09-java-interop/01-basics.md
  - docs/09-java-interop/02-advanced.md
  - docs/09-java-interop/03-libraries.md
  - docs/01-introduction/translation-tables.md
autonomous: true

must_haves:
  truths:
    - "Chapters 07, 08, and 09 each have a dedicated 'Reading Legacy Code' subpage at position 99 in the sidebar"
    - "Each legacy subpage shows 3+ legacy patterns with side-by-side modern vs legacy code blocks"
    - "Existing inline <details> legacy callouts in ch 07-09 subpages are replaced with one-line admonition links"
    - "A comprehensive translation reference page exists in the Introduction chapter with task-oriented rows covering Java, Python, C# to BBj mappings"
  artifacts:
    - path: "docs/07-file-io/02-legacy-code.md"
      provides: "Legacy file I/O patterns (direct access modes, string-packed records, IOLIST)"
      contains: "sidebar_position: 99"
    - path: "docs/08-database-sql/04-legacy-code.md"
      provides: "Legacy database patterns (SQL.INI, string concatenation SQL, MID extraction)"
      contains: "sidebar_position: 99"
    - path: "docs/09-java-interop/04-legacy-code.md"
      provides: "Legacy Java interop patterns (pre-use FQN, ADDR/CALL, string-based data exchange)"
      contains: "sidebar_position: 99"
    - path: "docs/01-introduction/translation-tables.md"
      provides: "Comprehensive Java/Python/C# to BBj translation reference organized by task"
      contains: "sidebar_position: 3"
  key_links:
    - from: "docs/08-database-sql/01-connecting.md"
      to: "docs/08-database-sql/04-legacy-code.md"
      via: "admonition link replacing <details> callout"
      pattern: "Reading Legacy Code"
    - from: "docs/01-introduction/translation-tables.md"
      to: "per-chapter docs"
      via: "task-oriented reference with cross-links"
      pattern: "translation"
---

<objective>
Create dedicated "Reading Legacy Code" subpages for chapters 07 (File I/O), 08 (Database/SQL), and 09 (Java Interop), replace their inline callouts with links, and create the comprehensive translation reference page in the Introduction chapter.

Purpose: Complete the legacy code training coverage across all data/application chapters and provide the "I know Java/Python/C#, how do I do this in BBj?" reference that serves as a landing page for experienced developers.

Output: 3 legacy code subpages + 7 updated chapter files + 1 comprehensive translation reference page.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-differentiators-and-quality/06-RESEARCH.md

Read the existing inline `<details>` callouts in chapters 07, 08, and 09 to incorporate and expand that content into the new subpages.

Chapter 07 (File I/O) is special: the entire chapter is "legacy-adjacent" since file I/O itself is largely a legacy pattern (SQL is the modern approach). The legacy subpage should focus on the MOST legacy patterns: direct access mode differences, string-packed records, and IOLIST templates.

Chapter 10 (Event Handling) already has a dedicated legacy page at `03-legacy.md` -- no new subpage needed.

Chapter 11 (Debugging) has only 2 borderline legacy patterns -- keep those as inline callouts, no dedicated subpage.

Translation tables use three source languages: Java, Python, C# (per user decision in CONTEXT.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create legacy code subpages for chapters 07, 08, 09 and replace inline callouts</name>
  <files>
    docs/07-file-io/02-legacy-code.md
    docs/07-file-io/index.md
    docs/08-database-sql/04-legacy-code.md
    docs/08-database-sql/01-connecting.md
    docs/08-database-sql/02-queries.md
    docs/08-database-sql/03-patterns.md
    docs/09-java-interop/04-legacy-code.md
    docs/09-java-interop/01-basics.md
    docs/09-java-interop/02-advanced.md
    docs/09-java-interop/03-libraries.md
  </files>
  <action>
**Create 3 legacy code subpages** using the same structure as Plan 01:

Frontmatter for each:
```yaml
---
sidebar_position: 99
title: "Reading Legacy Code"
description: "Recognize and understand legacy [topic] patterns in older BBj codebases"
---
```

Each entry uses the paired code block pattern: legacy code block, then modern code block, then 1-2 sentence explanation.

**Chapter 07 legacy subpage** (`02-legacy-code.md` -- position 02 since ch 07 only has index.md) -- entries:
1. Direct Access File Modes (Gen 1) -- DIRECT/SERIAL/INDEXED channel open modes vs SQL database access
2. String-Packed Records (Gen 1/2) -- manually packing fields into fixed-width strings vs templated DIM
3. IOLIST for Field Mapping (Gen 2/3) -- IOLIST clause on READ/WRITE for field layout vs SQL column names
4. Channel-Number File Management (Gen 1) -- UNT/OPEN/CLOSE channel tracking vs SQL connection lifecycle

**Chapter 08 legacy subpage** (`04-legacy-code.md`) -- entries:
1. SQL.INI Configuration (Gen 3) -- SQL.INI file for connection strings vs Enterprise Manager / config API
2. String-Concatenated SQL (Gen 3) -- building SQL with string concatenation vs SQLPREP with ? parameters
3. MID() Extraction from Result Strings (Gen 2/3) -- extracting fields via MID() position vs SQLTMPL + DIM dot notation
4. BBjRecordSet Direct Usage (Gen 3) -- BBjRecordSet navigation (first/next/eof) vs SQLFETCH loop

**Chapter 09 legacy subpage** (`04-legacy-code.md`) -- entries:
1. Pre-`use` Fully-Qualified Class Names (Gen 3) -- `new java.util.HashMap()` everywhere vs `use java.util.HashMap` + `new HashMap()`
2. ADDR() and CALL for Callbacks (Gen 2) -- ADDR()/CALL() function pointers vs method-based setCallback
3. String-Based Data Exchange (Gen 2/3) -- delimited strings as inter-module data transport vs passing Java/BBj objects
4. Procedural Java Interop (Gen 3) -- standalone Java calls without class wrapper vs encapsulating in Custom Object methods

**Replace inline `<details>` callouts** in the existing subpage files:

For each file with a legacy `<details>` block:
1. Find the entire `<details>...</details>` block
2. Replace with admonition link:
   ```markdown
   :::tip[Reading Legacy Code]
   See [Reading Legacy Code](./04-legacy-code) for the historical equivalents of the patterns on this page.
   :::
   ```
3. For ch 07, link to `./02-legacy-code` instead (different filename)

Read each existing file first to understand the inline callout content before creating the subpages. Incorporate and expand that content.
  </action>
  <verify>
3 new files exist with sidebar_position: 99 and 3+ pattern entries each. The 7 existing subpage files no longer contain `<details>` legacy callout blocks. Run `npm run build` to verify no broken links.
  </verify>
  <done>
Chapters 07, 08, and 09 each have a "Reading Legacy Code" subpage with 4 side-by-side legacy/modern code examples. All inline callouts are replaced with admonition links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive translation reference page</name>
  <files>docs/01-introduction/translation-tables.md</files>
  <action>
Create `docs/01-introduction/translation-tables.md` with frontmatter:
```yaml
---
sidebar_position: 3
title: "BBj for Java, Python, and C# Developers"
description: "Task-oriented translation tables mapping familiar Java, Python, and C# patterns to BBj equivalents"
---
```

Content structure:

1. Intro paragraph: "If you're coming from Java, Python, or C#, this page maps the patterns you already know to their BBj equivalents. Each row shows how to accomplish a common task across all four languages."

2. Organize by task category (H2 sections):

**Variables and Types:**
| Task | Java | Python | C# | BBj |
Rows: Declare string, declare number, declare boolean, null/None check, type conversion (string to number)

**Control Flow:**
| Task | Java | Python | C# | BBj |
Rows: If/else, for loop, while loop, for-each/iterate, switch/match

**Error Handling:**
| Task | Java | Python | C# | BBj |
Rows: Set error handler, catch specific error, catch all, get error message, raise/throw, skip on error, finally/cleanup

**Strings:**
| Task | Java | Python | C# | BBj |
Rows: Get length, substring, find in string, replace, split, trim, regex match, case conversion

**Collections:**
| Task | Java | Python | C# | BBj |
Rows: Create list/array, add item, get by index, iterate, create map/dict, get by key, check contains, get size

**Classes and Objects:**
| Task | Java | Python | C# | BBj |
Rows: Define class, constructor, instance method, static method, inheritance, interface, create instance

**File and Database:**
| Task | Java | Python | C# | BBj |
Rows: Open DB connection, execute query, iterate results, close connection, read file, write file

**Events and Callbacks:**
| Task | Java | Python | C# | BBj |
Rows: Register event handler, handle button click, event loop

Keep each section to 5-8 rows. Total page should be ~20-30 task rows across all categories. Use concise code snippets in table cells (single line where possible). Where BBj differs fundamentally (e.g., no try/catch), add a brief footnote.

3. Closing note: "For detailed examples of each BBj pattern, follow the links in the tutorial chapters. This page is a quick-reference starting point."
  </action>
  <verify>
File exists at `docs/01-introduction/translation-tables.md` with sidebar_position: 3. Contains at least 6 task category sections with 4-column translation tables. Each table has Java, Python, C#, and BBj columns. Run `npm run build` to verify the page renders and no broken links.
  </verify>
  <done>
A comprehensive translation reference page exists with task-oriented tables mapping Java, Python, and C# patterns to BBj across 7+ task categories. Developers coming from other languages can use it as a quick-lookup aid.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no broken link errors
2. 3 legacy subpages exist for ch 07, 08, 09 with sidebar_position: 99 and 3+ entries each
3. No remaining `<details>` legacy callout blocks in ch 07-09 subpages
4. Translation reference page at `docs/01-introduction/translation-tables.md` has 7+ task category tables
5. All admonition links in modified files point to valid relative paths
</verification>

<success_criteria>
- Legacy code training is complete across chapters 04-09 (6 dedicated subpages total with Plans 01+02)
- The translation reference page serves the "I know Java, how do I do this in BBj?" use case with task-oriented tables
- Build passes with no broken links
</success_criteria>

<output>
After completion, create `.planning/phases/06-differentiators-and-quality/06-02-SUMMARY.md`
</output>
