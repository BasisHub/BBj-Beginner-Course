---
phase: 06-differentiators-and-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/01-introduction/generations.md
  - docs/04-error-handling/04-legacy-code.md
  - docs/04-error-handling/01-seterr-and-err.md
  - docs/04-error-handling/02-throw-and-custom.md
  - docs/04-error-handling/03-patterns.md
  - docs/05-strings-and-numbers/04-legacy-code.md
  - docs/05-strings-and-numbers/01-string-basics.md
  - docs/05-strings-and-numbers/02-searching.md
  - docs/05-strings-and-numbers/03-formatting.md
  - docs/06-collections/04-legacy-code.md
  - docs/06-collections/01-bbjvector.md
  - docs/06-collections/02-java-collections.md
  - docs/06-collections/03-arrays-conversion.md
autonomous: true

must_haves:
  truths:
    - "A 'Generations of BBj' page in the Introduction chapter shows 4 generations side-by-side with era, product, signature syntax, and modern equivalent"
    - "Chapters 04, 05, and 06 each have a dedicated 'Reading Legacy Code' subpage at position 99 in the sidebar"
    - "Each legacy subpage shows 3+ legacy patterns with side-by-side modern vs legacy code blocks"
    - "Existing inline <details> legacy callouts in ch 04-06 subpages are replaced with one-line links to the new legacy subpage"
  artifacts:
    - path: "docs/01-introduction/generations.md"
      provides: "4-generation BBj reference with comparison table"
      contains: "BBx"
    - path: "docs/04-error-handling/04-legacy-code.md"
      provides: "Legacy error handling patterns (line numbers, ON ERR GOTO, STBL)"
      contains: "sidebar_position: 99"
    - path: "docs/05-strings-and-numbers/04-legacy-code.md"
      provides: "Legacy string/number patterns (uppercase keywords, line numbers, inline PRINT masks)"
      contains: "sidebar_position: 99"
    - path: "docs/06-collections/04-legacy-code.md"
      provides: "Legacy collection patterns (DIM-as-collection, BBjHashMap, REDIM)"
      contains: "sidebar_position: 99"
  key_links:
    - from: "docs/04-error-handling/01-seterr-and-err.md"
      to: "docs/04-error-handling/04-legacy-code.md"
      via: "inline link replacing <details> callout"
      pattern: "Reading Legacy Code"
    - from: "docs/01-introduction/generations.md"
      to: "docs/04-error-handling/04-legacy-code.md"
      via: "cross-link from generation description"
      pattern: "legacy"
---

<objective>
Create the "Generations of BBj" reference page and dedicated "Reading Legacy Code" subpages for chapters 04 (Error Handling), 05 (Strings and Numbers), and 06 (Collections). Replace existing inline `<details>` legacy callouts in those chapters with one-line links to the new subpages.

Purpose: Provide systematic legacy code recognition training so developers encountering old BBj code can identify which era it's from and know the modern equivalent. The generations page is the "decoder ring"; the per-chapter subpages are the detailed recognition guides.

Output: 1 generations reference page + 3 legacy code subpages + 9 updated chapter files with inline callouts replaced by links.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-differentiators-and-quality/06-RESEARCH.md

Read the existing inline `<details>` callouts in chapters 04, 05, and 06 to understand the legacy content that already exists. The new subpages should incorporate and expand this content, not duplicate it.

Key BBj generation info from research:
- Gen 1: BBx (1985-1995) -- line numbers, GOTO, DIM arrays, character-mode I/O
- Gen 2: PRO/5 / Visual PRO/5 (1995-2002) -- CALLBACK verb, SYSGUI channel, READ RECORD events
- Gen 3: Early BBj 1.x-5.x (2002-2006) -- Java `new`, fully-qualified class names, SQLOPEN/SQL verbs
- Gen 4: Modern BBj 6.0+ (2006-present) -- class/classend, setCallback, `use`, Custom Objects
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Generations of BBj reference page</name>
  <files>docs/01-introduction/generations.md</files>
  <action>
Create `docs/01-introduction/generations.md` with frontmatter:
```yaml
---
sidebar_position: 2
title: "Generations of BBj"
description: "A decoder ring for identifying and understanding the four generations of BBj code"
---
```

Content structure:
1. Brief intro paragraph: "BBj code you encounter will come from one of four generations. Each generation has distinctive syntax patterns. This page helps you identify which era code was written in and understand its modern equivalent."

2. Central comparison table with columns: Generation | Era | Product | Signature Syntax | Modern Equivalent
   - Gen 1: BBx (1985-1995), line numbers + GOTO + DIM arrays, labels + BBjVector + GUI
   - Gen 2: PRO/5 (1995-2002), CALLBACK verb + SYSGUI channel + READ RECORD, setCallback + process_events
   - Gen 3: Early BBj (2002-2006), Java new + FQN + SQLOPEN, `use` imports + Custom Objects
   - Gen 4: Modern BBj 6.0+ (2006-present), class/classend + method/field + setCallback methods, current recommended style

3. Expanded section for each generation (H2) with:
   - 2-3 sentence description of the era and what to expect
   - One short representative code block (3-6 lines) showing the signature style
   - "See also" link to relevant per-chapter legacy subpages (cross-link to ch 04-09 legacy subpages)

4. Closing paragraph: "Most codebases you encounter will mix generations. A program might use Gen 4 class syntax but still contain Gen 1 DIM arrays or Gen 2 CALLBACK verbs. The per-chapter legacy guides linked above show you exactly what each pattern looks like and its modern replacement."

Cross-link to legacy subpages using relative paths. Include links to ALL legacy subpages that will exist after Plan 02 completes (ch 04-09), not just ch 04-06. Use the path pattern `../04-error-handling/04-legacy-code` etc.

For chapters 10 (Event Handling), link to the existing `../10-event-handling/03-legacy` page instead of a new legacy subpage.
  </action>
  <verify>
File exists at docs/01-introduction/generations.md with sidebar_position: 2 in frontmatter. Contains a 4-row comparison table. Contains H2 sections for each generation with code blocks. Contains cross-links to chapter legacy subpages.
  </verify>
  <done>
The Generations page shows all 4 BBj generations side-by-side with era, product, signature syntax, modern equivalent, and cross-links to per-chapter legacy guides.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create legacy code subpages for chapters 04, 05, 06 and replace inline callouts</name>
  <files>
    docs/04-error-handling/04-legacy-code.md
    docs/05-strings-and-numbers/04-legacy-code.md
    docs/06-collections/04-legacy-code.md
    docs/04-error-handling/01-seterr-and-err.md
    docs/04-error-handling/02-throw-and-custom.md
    docs/04-error-handling/03-patterns.md
    docs/05-strings-and-numbers/01-string-basics.md
    docs/05-strings-and-numbers/02-searching.md
    docs/05-strings-and-numbers/03-formatting.md
    docs/06-collections/01-bbjvector.md
    docs/06-collections/02-java-collections.md
    docs/06-collections/03-arrays-conversion.md
  </files>
  <action>
**Create 3 legacy code subpages** with this common structure:

Frontmatter for each:
```yaml
---
sidebar_position: 99
title: "Reading Legacy Code"
description: "Recognize and understand legacy [topic] patterns in older BBj codebases"
---
```

Each subpage opens with: "You'll encounter these patterns in older BBj codebases. Each section shows the legacy approach and its modern equivalent."

Each entry on the page uses this pattern:
```markdown
### [Pattern Name]

You may see this in [era] code:

```bbj
rem Legacy: [brief description]
[legacy code, 3-8 lines]
```

The modern equivalent:

```bbj
rem Modern: [brief description]
[modern code, 3-8 lines]
```

[1-2 sentence explanation of why the old pattern exists and why modern is preferred]
```

**Chapter 04 legacy subpage** (`04-legacy-code.md`) -- entries:
1. Line-Number Error Trapping (Gen 1) -- `seterr 9500` with line numbers vs label-based seterr
2. ON ERR GOTO / ON ERR GOSUB (Gen 1/2) -- ON ERR GOTO linenum vs seterr label
3. !THROWERROR STBL Pattern (Gen 3) -- STBL("!THROWERROR","TRUE") before THROW existed
4. Error Handling Without THROW (Gen 2/3) -- escape/release based recovery vs THROW + structured handling

**Chapter 05 legacy subpage** (`04-legacy-code.md`) -- entries:
1. Uppercase Keywords and Line Numbers (Gen 1) -- `0010 PRINT "HELLO"` vs lowercase modern style
2. String Searching Before MASK() (Gen 1/2) -- POS() workarounds for pattern matching vs MASK() regex
3. Inline PRINT Formatting Masks (Gen 1/2) -- `PRINT USING "##,###.00", x` vs STR(x, "##,###.00")
4. MID$/LEFT$/RIGHT$ Functions (migrant pattern) -- BASIC dialect functions vs BBj substring notation `str$(1,5)`

**Chapter 06 legacy subpage** (`04-legacy-code.md`) -- entries:
1. DIM Arrays as Collections (Gen 1) -- oversized DIM + counter variable vs BBjVector
2. BBjHashMap (deprecated, Gen 3) -- BBjHashMap() constructor vs java.util.HashMap
3. REDIM for Array Resizing (Gen 1/2) -- REDIM to grow arrays vs BBjVector.addItem()
4. Parallel Arrays (Gen 1) -- synchronized DIM arrays for key-value pairs vs HashMap

**Replace inline `<details>` callouts** in the 9 existing subpage files:

For each file that has a `<details>` block with "Reading Legacy Code" or similar legacy content:
1. Find the entire `<details>...</details>` block
2. Replace it with a single line:
   ```markdown
   :::tip[Reading Legacy Code]
   See [Reading Legacy Code](./04-legacy-code) for the historical equivalents of the patterns on this page.
   :::
   ```
3. Use the Docusaurus admonition syntax (:::tip) for visual consistency
4. Adjust the relative link path based on the file's location

Read each of the 9 existing files first to find and understand the inline callout content before creating the legacy subpages. The subpage content should incorporate the existing callout content (expand it, don't just copy it).
  </action>
  <verify>
3 new files exist: `docs/04-error-handling/04-legacy-code.md`, `docs/05-strings-and-numbers/04-legacy-code.md`, `docs/06-collections/04-legacy-code.md`. Each has sidebar_position: 99 and at least 3 pattern entries with paired code blocks. The 9 existing subpage files no longer contain `<details>` legacy callout blocks -- they contain admonition links instead. Run `npm run build` to verify no broken links.
  </verify>
  <done>
Chapters 04, 05, and 06 each have a "Reading Legacy Code" subpage with 3-4 side-by-side legacy/modern code examples. All inline callouts are replaced with admonition links. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no broken link errors
2. `docs/01-introduction/generations.md` contains a 4-generation comparison table
3. Each of the 3 legacy subpages has `sidebar_position: 99` and 3+ pattern entries
4. No remaining `<details>` legacy callout blocks in ch 04-06 subpages (replaced with admonition links)
5. Cross-links from generations page resolve to valid paths
</verification>

<success_criteria>
- The Generations page works as a "decoder ring" -- a developer can look at old code, identify its generation, and understand the modern equivalent
- Legacy subpages in ch 04-06 provide recognition-only depth: "You'll see this. Here's the modern way."
- Inline callouts are cleanly replaced with links, keeping main content focused on modern patterns
- Build passes with no broken links
</success_criteria>

<output>
After completion, create `.planning/phases/06-differentiators-and-quality/06-01-SUMMARY.md`
</output>
