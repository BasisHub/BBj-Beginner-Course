---
phase: 06-differentiators-and-quality
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/04-error-handling/index.md
  - docs/05-strings-and-numbers/index.md
  - docs/06-collections/index.md
  - docs/08-database-sql/index.md
  - docs/09-java-interop/index.md
  - docs/10-event-handling/index.md
  - docs/11-debugging/index.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Each chapter with sample .bbj files has a visible callout directing readers to the complete runnable versions"
    - "A reader encountering a code fragment in a chapter can quickly find the corresponding complete sample file"
    - "The site builds cleanly after all changes"
  artifacts:
    - path: "docs/04-error-handling/index.md"
      provides: "Sample files callout referencing samples/04-error-handling/"
      contains: "samples"
    - path: "docs/05-strings-and-numbers/index.md"
      provides: "Sample files callout referencing samples/05-strings-and-numbers/"
      contains: "samples"
    - path: "docs/06-collections/index.md"
      provides: "Sample files callout referencing samples/06-collections/"
      contains: "samples"
    - path: "docs/08-database-sql/index.md"
      provides: "Sample files callout referencing samples/08-database-sql/"
      contains: "samples"
    - path: "docs/09-java-interop/index.md"
      provides: "Sample files callout referencing samples/09-java-interop/"
      contains: "samples"
    - path: "docs/10-event-handling/index.md"
      provides: "Sample files callout referencing samples/10-event-handling/"
      contains: "samples"
    - path: "docs/11-debugging/index.md"
      provides: "Sample files callout referencing samples/11-debugging/"
      contains: "samples"
  key_links:
    - from: "docs/*/index.md"
      to: "samples/*/*.bbj"
      via: "Admonition callout with file listing"
      pattern: "samples/"
---

<objective>
Add explicit sample file references to each chapter that has runnable .bbj files in the samples/ directory, closing the verification gap where inline code fragments lack full program structure.

Purpose: Readers encountering instructional code fragments (e.g., single-statement examples without `release`/`end`) can immediately find corresponding complete, runnable sample programs. This closes verification gap 2 ("Every code example is copy-paste runnable") by ensuring each chapter prominently links to its complete sample files.

Output: 7 updated chapter index.md files with sample file callouts, plus a clean build verification.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-differentiators-and-quality/06-VERIFICATION.md
@.planning/phases/06-differentiators-and-quality/06-CONTEXT.md
@docs/samples.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sample file callouts to 7 chapter index pages</name>
  <files>
    docs/04-error-handling/index.md
    docs/05-strings-and-numbers/index.md
    docs/06-collections/index.md
    docs/08-database-sql/index.md
    docs/09-java-interop/index.md
    docs/10-event-handling/index.md
    docs/11-debugging/index.md
  </files>
  <action>
    Add a Docusaurus admonition callout near the end of each chapter's index.md (before the translation table section or as the final section before any legacy code link) that lists the complete runnable sample files for that chapter.

    Use this pattern for each chapter:

    ```markdown
    :::tip[Complete Runnable Examples]
    This chapter's code snippets illustrate individual concepts. For complete, runnable programs you can open directly in the BBj IDE, see the sample files in [`samples/{chapter-dir}/`](https://github.com/BasisHub/BBj-Beginner-Course/tree/master/samples/{chapter-dir}):

    - `{filename1}.bbj` -- {brief description from the rem header}
    - `{filename2}.bbj` -- {brief description}
    - ...

    See [Running Samples](/samples) for setup instructions.
    :::
    ```

    Specific files per chapter:

    **04-error-handling** (4 files):
    - `seterr_basic.bbj` -- Basic SETERR error trapping
    - `err_clause.bbj` -- ERR= clause on statements
    - `throw_validation.bbj` -- THROW for input validation
    - `java_error_handling.bbj` -- Java exception handling from BBj

    **05-strings-and-numbers** (4 files):
    - `string_basics.bbj` -- String functions (LEN, MID, CVS, POS)
    - `pos_searching.bbj` -- POS() pattern searching
    - `str_num_formatting.bbj` -- STR() and NUM() formatting
    - `mask_regex.bbj` -- MASK() pattern matching

    **06-collections** (4 files):
    - `bbjvector_basics.bbj` -- BBjVector creation and iteration
    - `arraylist_usage.bbj` -- Java ArrayList from BBj
    - `hashmap_iterator.bbj` -- HashMap with iterator pattern
    - `array_to_vector.bbj` -- Converting DIM arrays to vectors

    **08-database-sql** (4 files):
    - `sqlopen_basic.bbj` -- Opening a SQL connection
    - `select_query.bbj` -- SELECT with SQLFETCH loop
    - `insert_update.bbj` -- INSERT and UPDATE operations
    - `sql_patterns.bbj` -- Common SQL patterns and error handling

    **09-java-interop** (5 files):
    - `java_basics.bbj` -- Basic Java class usage from BBj
    - `java_interfaces.bbj` -- Implementing Java interfaces
    - `http_request.bbj` -- HTTP requests using Java libraries
    - `json_example.bbj` -- JSON parsing with org.json
    - `utility_examples.bbj` -- Java utility class usage

    **10-event-handling** (4 files):
    - `setcallback_labels.bbj` -- setCallback with label targets
    - `setcallback_methods.bbj` -- setCallback with method targets
    - `event_types.bbj` -- Common BBj event types
    - `contact_form.bbj` -- Complete contact form example

    **11-debugging** (3 files):
    - `settrace_example.bbj` -- SETTRACE output demonstration
    - `dump_example.bbj` -- DUMP variable inspection
    - `logging_example.bbj` -- PRINT-based logging patterns

    IMPORTANT: Read each file's rem === header to get the actual description before writing. The descriptions above are best guesses -- use the real header text from each .bbj file.

    IMPORTANT: Read each index.md first to find the right insertion point. Place the callout:
    - AFTER the main content sections
    - BEFORE the "Reading Legacy Code" tip/link (the :::tip that links to ./legacy-code)
    - If no legacy code link exists, place it as the last section before any frontmatter-generated content

    Do NOT modify any other content in the files. This is an additive change only.
  </action>
  <verify>
    For each of the 7 files, grep for "samples/" to confirm the callout was added:
    ```bash
    grep -l "samples/" docs/04-error-handling/index.md docs/05-strings-and-numbers/index.md docs/06-collections/index.md docs/08-database-sql/index.md docs/09-java-interop/index.md docs/10-event-handling/index.md docs/11-debugging/index.md
    ```
    All 7 files should appear in the output.
  </verify>
  <done>All 7 chapter index pages contain an admonition callout listing their sample .bbj files with descriptions and a link to the Running Samples page</done>
</task>

<task type="auto">
  <name>Task 2: Build verification</name>
  <files>None (verification only)</files>
  <action>
    Run `npm run build` in the project root to verify the site builds cleanly after the sample callout additions. Check for:
    - Zero build errors
    - No broken internal links (Docusaurus reports these as build errors)
    - No new warnings introduced

    If the build fails, diagnose and fix the issue (likely a markdown syntax problem in one of the callouts).
  </action>
  <verify>
    ```bash
    npm run build
    ```
    Exit code 0, no errors in output.
  </verify>
  <done>Site builds successfully with zero errors after all sample file callout additions</done>
</task>

</tasks>

<verification>
1. All 7 chapters with sample files (04, 05, 06, 08, 09, 10, 11) have a visible callout directing readers to complete runnable examples
2. Each callout lists the specific .bbj files with accurate descriptions from the file headers
3. Each callout links to the GitHub repository samples directory and the Running Samples page
4. `npm run build` succeeds with zero errors
</verification>

<success_criteria>
- grep "samples/" across all 7 chapter index files returns matches
- npm run build exits cleanly
- A reader on any chapter page can see where to find complete runnable code
</success_criteria>

<output>
After completion, create `.planning/phases/06-differentiators-and-quality/06-04-SUMMARY.md`
</output>
