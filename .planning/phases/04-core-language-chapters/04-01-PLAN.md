---
phase: 04-core-language-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/04-error-handling/index.md
  - docs/04-error-handling/01-seterr-and-err.md
  - docs/04-error-handling/02-throw-and-custom.md
  - docs/04-error-handling/03-patterns.md
  - samples/04-error-handling/seterr_basic.bbj
  - samples/04-error-handling/err_clause.bbj
  - samples/04-error-handling/throw_validation.bbj
  - samples/04-error-handling/java_error_handling.bbj
autonomous: true

must_haves:
  truths:
    - "A reader can look up SETERR and understand how to set a global error trap with labels"
    - "A reader can use ERR= clauses on individual statements to handle errors locally"
    - "A reader can use THROW to raise custom errors with codes 256-1024"
    - "A reader can use ERR() and ERRMES() to inspect the last error"
    - "A reader understands the error trapping priority hierarchy (END= > DOM= > function ERR= > statement ERR= > SETERR)"
    - "A reader can handle Java exceptions from BBj code using ERR= and error 252"
    - "Legacy code callouts show line-number SETERR and ON ERR GOTO patterns from earlier generations"
  artifacts:
    - path: "docs/04-error-handling/index.md"
      provides: "Chapter landing page with quick-reference table"
      contains: "At a Glance"
    - path: "docs/04-error-handling/01-seterr-and-err.md"
      provides: "SETERR, ERR=, ERR(), ERRMES(), error codes, priority hierarchy"
      contains: "seterr"
    - path: "docs/04-error-handling/02-throw-and-custom.md"
      provides: "THROW verb, custom error range 256-1024, RETRY, error propagation"
      contains: "throw"
    - path: "docs/04-error-handling/03-patterns.md"
      provides: "ERR=*NEXT, Java exception handling, cleanup patterns"
      contains: "err=\\*next"
    - path: "samples/04-error-handling/seterr_basic.bbj"
      provides: "Runnable SETERR example"
    - path: "samples/04-error-handling/throw_validation.bbj"
      provides: "Runnable THROW from class example"
  key_links:
    - from: "docs/04-error-handling/index.md"
      to: "subpages 01, 02, 03"
      via: "Docusaurus autogenerated sidebar picks up all .md files in directory"
      pattern: "sidebar_position"
    - from: "docs/04-error-handling/03-patterns.md"
      to: "docs/03-object-oriented/02-using-java.md"
      via: "Cross-link to Java interop chapter for context on Java exceptions"
      pattern: "using-java"
---

<objective>
Create the Error Handling chapter with index page and three subpages covering SETERR/ERR=, THROW/custom errors, and practical patterns.

Purpose: Error handling is the first thing developers need after basic syntax -- every BBj program must handle errors, and the patterns (SETERR, ERR=, THROW) are unique to BBj. This chapter fills the biggest daily-use gap in the current tutorial.
Output: Rewritten `docs/04-error-handling/index.md`, three new subpage files, and runnable .bbj sample files in `samples/04-error-handling/`.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-language-chapters/04-CONTEXT.md
@.planning/phases/04-core-language-chapters/04-RESEARCH.md
@docs/04-error-handling/index.md
@docs/03-object-oriented/index.md (pattern reference for chapter landing page)
@docs/03-object-oriented/01-writing-classes.md (pattern reference for subpage structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite error handling index.md as chapter landing page</name>
  <files>docs/04-error-handling/index.md</files>
  <action>
Replace the placeholder content of `docs/04-error-handling/index.md` with a chapter landing page:

1. **Frontmatter:** Keep `sidebar_position: 1` and `title: "Error Handling"`

2. **Heading:** `# Error Handling`

3. **Opening paragraph (2-3 sentences):** BBj does not have try/catch. Instead, it uses a trap-and-branch model: you set error traps (SETERR, ERR=) that redirect execution when errors occur, and raise errors with THROW. This is the pattern used in all BBj code -- understanding it is essential before writing anything non-trivial.

4. **"At a Glance" quick-reference table:**

| Feature | Syntax | Purpose |
|---------|--------|---------|
| SETERR | `seterr label` | Set global error trap |
| ERR= | `open(1,err=handler)"file"` | Statement-level error trap |
| THROW | `throw "message", 256` | Raise custom error |
| ERR | `err` | Last error number |
| ERRMES | `errmes(-1)` | Last error message |
| RETRY | `retry` | Re-execute the statement that errored |

5. **No other content** -- Docusaurus autogenerated sidebar handles navigation to subpages.

IMPORTANT: Do NOT mention try/catch anywhere. BBj does NOT have try/catch/endtry. This is a verified fact from research (the existing tutorial's try/catch example fails compilation).
  </action>
  <verify>
- File contains "At a Glance" table with SETERR, ERR=, THROW, ERR, ERRMES, RETRY
- File does NOT contain "try", "catch", or "endtry"
- Has `sidebar_position: 1` in frontmatter
- Short landing page (under 40 lines of content)
  </verify>
  <done>Error handling index.md is a focused landing page with quick-reference table, no try/catch references.</done>
</task>

<task type="auto">
  <name>Task 2: Create three error handling subpages and sample files</name>
  <files>docs/04-error-handling/01-seterr-and-err.md, docs/04-error-handling/02-throw-and-custom.md, docs/04-error-handling/03-patterns.md, samples/04-error-handling/seterr_basic.bbj, samples/04-error-handling/err_clause.bbj, samples/04-error-handling/throw_validation.bbj, samples/04-error-handling/java_error_handling.bbj</files>
  <action>
Create three subpage files and corresponding sample .bbj files. All code examples must use ONLY patterns verified in 04-RESEARCH.md. NEVER use try/catch/endtry syntax.

**File 1: `docs/04-error-handling/01-seterr-and-err.md`**

Frontmatter: `sidebar_position: 1`, `title: "SETERR and Error Trapping"`

Content sections:
1. **Opening (2-3 sentences):** SETERR is BBj's global error trap -- when an error occurs anywhere in the program, execution branches to the specified label. ERR= provides statement-level trapping. Together they form the foundation of BBj error handling.
2. **SETERR with Labels:** Show the basic pattern: `seterr handler` ... `handler:` block. Use the verified example from RESEARCH.md (seterr handler, x = num("not a number"), handler prints err and errmes(-1)). Explain that SETERR is reset to 0 after branching (this is a known gotcha from research Pitfall 4).
3. **ERR= on Statements:** Show ERR= on `num()` (err=*next to skip), ERR= on `open()` (err=fileErr label). Use the verified examples from RESEARCH.md.
4. **Inspecting Errors (ERR and ERRMES):** `err` returns the last error number (0-255 system, 256-1024 developer range). `errmes(-1)` returns the last error message. `errmes(code)` returns the message for a specific error code.
5. **Error Trapping Priority:** Teach the five-level priority hierarchy from research: END= > DOM= > function ERR= > statement ERR= > SETERR. Brief table showing the hierarchy. Explain that more specific traps override less specific ones.
6. **Common Error Codes:** Show a table of the most frequently encountered error codes (0=ERR FPTR, 11=record not found, 12=file not open, 14=end of file, 17=file does not exist, 41=invalid assignment, 42=structure of statement, 43=use of undefined string variable, 252=Java exception). Link to the full [List of Errors](https://documentation.basis.cloud/BASISHelp/WebHelp/usr/Errors/Errors.htm).

Legacy callout (collapsible `<details>`): "Reading Legacy Code: Line-Number Error Trapping"
- Show `SETERR 9500` with numbered lines pattern vs modern `seterr handler` with label
- Show `ON ERR GOTO 9500` pattern
- Brief note: "Both patterns still work in BBj. The behavior is identical to label-based trapping."

Further Reading (:::tip admonition):
- [SETERR Verb](https://documentation.basis.cloud/BASISHelp/WebHelp/commands/seterr_verb.htm)
- [Error Trapping Rules](https://documentation.basis.cloud/BASISHelp/WebHelp/usr/Language_Concepts/error_trapping_rules.htm)
- [List of Errors](https://documentation.basis.cloud/BASISHelp/WebHelp/usr/Errors/Errors.htm)

**File 2: `docs/04-error-handling/02-throw-and-custom.md`**

Frontmatter: `sidebar_position: 2`, `title: "THROW and Custom Errors"`

Content sections:
1. **Opening (2-3 sentences):** THROW lets you raise your own errors with custom messages and codes. Combined with SETERR trapping, this gives you structured error propagation through the call stack -- BBj's equivalent of throwing exceptions.
2. **THROW Syntax:** `throw "message", code` where code is in the developer range 256-1024. Show a simple standalone example.
3. **THROW from Class Methods:** Use the Validator class example from RESEARCH.md. Show how THROW in a method propagates up to the caller's SETERR handler.
4. **Custom Error Messages (ERRMES writable):** Show `errmes(256, "Custom message")` to register custom error messages that persist.
5. **RETRY Verb:** RETRY re-executes the statement that caused the error. Explain that RETRY also restores the saved SETERR value (from research Pitfall 4). Show a practical retry pattern (e.g., retry a file open after prompting for a new path).
6. **Error Propagation Through the Call Stack:** When THROW is called in a method and the method has no SETERR, the error propagates to the caller. Walk through the flow briefly.

Legacy callout: "Reading Legacy Code: Error Handling Before THROW"
- Before BBj added THROW, programs could not raise custom errors
- `!THROWERROR` STBL (BBj 6.0+) was an early mechanism for error propagation in class methods
- Brief note on `!ERROR_HANDLER` STBL (BBj 17.0+) for program-level untrapped error handlers

Further Reading:
- [Custom Objects Tutorial: Error Handling](https://documentation.basis.cloud/BASISHelp/WebHelp/tutorials/custom_objects/custom_objects_10program4.htm)
- [ERRMES() Function](https://documentation.basis.cloud/BASISHelp/WebHelp/commands/errmes_function.htm)
- [STBL Formats (THROWERROR)](https://documentation.basis.cloud/BASISHelp/WebHelp/usr/BBj_Enhancements/stbl_formats_bbj.htm)

**File 3: `docs/04-error-handling/03-patterns.md`**

Frontmatter: `sidebar_position: 3`, `title: "Common Error Handling Patterns"`

Content sections:
1. **Opening (2-3 sentences):** This section covers practical patterns you will use daily: skipping errors on non-critical operations, handling Java exceptions, and structuring cleanup code.
2. **ERR=*NEXT (Skip on Error):** Show `x = num(input$, err=*next)` pattern. Explain *NEXT means "continue with the next statement." Use for non-critical conversions where a default is acceptable.
3. **Handling Java Exceptions (Error 252):** When a Java method called from BBj throws a Java exception, BBj traps it as error 252. Use the verified HashMap example from RESEARCH.md. Show how to get exception details with `BBjAPI().getLastJavaException()`. Cross-link to the [Java interop chapter](/object-oriented/using-java) for broader context.
4. **Cleanup Patterns:** Show a pattern for ensuring resources are released: open a file, do work in a section protected by SETERR, the error handler closes the file before releasing. Emphasize: because SETERR resets after branching, re-establish SETERR if your handler does complex work.
5. **Choosing Between SETERR and ERR=:** When to use each:
   - SETERR for program-wide or method-wide "catch all" trapping
   - ERR= for specific statements where you expect errors and want local handling
   - ERR=*NEXT when you want to silently skip errors
   Show a brief comparison.

Legacy callout: "Reading Legacy Code: Error Handling with ON ERR"
- `ON ERR GOTO label` was an alternative to SETERR in early code
- Functionally identical to SETERR in modern BBj
- `ON ERR GOSUB label` variant used RETURN instead of RETRY

Further Reading:
- [BBj Object Error Handling](https://documentation.basis.cloud/BASISHelp/WebHelp/gridctrl/error_handling.htm)
- [BBjAPI::getLastJavaException](https://documentation.basis.cloud/BASISHelp/WebHelp/bbjobjects/bbjapi/BBjAPI_getLastBBjException.htm)
- [ERR() Function](https://documentation.basis.cloud/BASISHelp/WebHelp/commands/variables/err_function_variable.htm)

**Sample files** (create `samples/04-error-handling/` directory):

- `seterr_basic.bbj`: The SETERR + label example from RESEARCH.md (seterr handler, num("not a number"), handler prints err/errmes)
- `err_clause.bbj`: ERR= on num() and open() from RESEARCH.md
- `throw_validation.bbj`: The Validator class with THROW from RESEARCH.md
- `java_error_handling.bbj`: The HashMap ERR= example from RESEARCH.md

Each sample file must have a `rem` comment header explaining what it demonstrates. Each must be self-contained and runnable.

**Tone and style rules (all files):**
- Peer tone -- writing for experienced programmers who know error handling concepts but not BBj syntax
- Context first -- 2-3 sentence intro before any code
- Self-contained snippets -- each example demonstrates one concept independently
- Generic variable names -- focus on syntax, not business domain
- Show expected output for key examples (comment after print statements)
- All code blocks use ```bbj language tag
  </action>
  <verify>
- All three .md files exist with correct sidebar_position (1, 2, 3)
- All four sample .bbj files exist in samples/04-error-handling/
- ZERO mentions of "try", "catch", or "endtry" in any file
- Each subpage has: opening paragraph, code examples, legacy callout in `<details>`, Further Reading
- `01-seterr-and-err.md` contains error trapping priority table
- `02-throw-and-custom.md` contains Validator class example with THROW
- `03-patterns.md` contains ERR=*NEXT and error 252 (Java exception) patterns
- `npm run build` succeeds
  </verify>
  <done>Three error handling subpages cover SETERR/ERR= foundations, THROW/custom errors, and daily patterns. Four runnable sample files in samples/04-error-handling/. Legacy callouts cover line-number era and pre-THROW era. No try/catch anywhere.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. Sidebar shows "Error Handling" category with index + 3 subpages in order
3. grep -r "try\|catch\|endtry" docs/04-error-handling/ returns zero matches (CRITICAL)
4. Each subpage has `<details>` legacy callout and Further Reading section
5. All code blocks use ```bbj language tag
6. All sample files in samples/04-error-handling/ have rem comment headers
7. Cross-link to Java interop chapter exists in 03-patterns.md
</verification>

<success_criteria>
- Error handling chapter teaches SETERR, ERR=, THROW, ERR(), ERRMES() with working examples
- Error trapping priority hierarchy is documented
- Java exception handling (error 252) is covered
- Legacy callouts show line-number SETERR, ON ERR GOTO, and pre-THROW patterns
- No try/catch syntax anywhere (compiler-verified anti-pattern from research)
- All sample .bbj files are self-contained and runnable
- Build passes and sidebar navigation works
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-language-chapters/04-01-SUMMARY.md`
</output>
